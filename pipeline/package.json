{
  "name": "hantavirus-pipeline",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hantavirus Tracker ingestion + extraction + DB pipeline (Phase 0 onward)",
  "scripts": {
    "smoke": "tsx src/smoke-test.ts",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio",
    "db:reset": "prisma migrate reset",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "prisma": "^6.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
